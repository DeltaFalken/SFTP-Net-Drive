<Window x:Class="SftpNetDrive.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:SftpNetDrive.Localization"
        Title="{x:Static loc:Strings.AppTitle}"
        Width="680" Height="480"
        MinWidth="540" MinHeight="340"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3"
        Closing="Window_Closing">

    <Window.Resources>
        <SolidColorBrush x:Key="MountedBrush"    Color="#0F7B0F"/>
        <SolidColorBrush x:Key="ConnectingBrush"  Color="#E8A000"/>
        <SolidColorBrush x:Key="ErrorBrush"       Color="#C42B1C"/>
        <SolidColorBrush x:Key="UnmountedBrush"   Color="#767676"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="12 4"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E8E8E8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#0067C0"/>
            <Setter Property="BorderBrush" Value="#0067C0"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0078D4"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005499"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="BorderBrush" Value="#C42B1C"/>
            <Setter Property="Foreground" Value="#C42B1C"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0 0 0 1">
            <Grid Margin="16 0">
                <TextBlock Text="{x:Static loc:Strings.AppTitle}" FontSize="18" FontWeight="SemiBold"
                           VerticalAlignment="Center" Foreground="#1A1A1A"/>
                <Button x:Name="AddButton" Content="{x:Static loc:Strings.NewConnection}"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Padding="14 4" Click="AddButton_Click"/>
            </Grid>
        </Border>

        <!-- Connection list -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="12 8">
            <ItemsControl x:Name="ProfileList" VirtualizingPanel.IsVirtualizing="True">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0 4" Background="White" BorderBrush="#E0E0E0"
                                BorderThickness="1" CornerRadius="6" Padding="12 10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="32"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Drive letter badge -->
                                <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                                        Background="#EFF6FC" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DriveLetter}" FontSize="13" FontWeight="Bold"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Foreground="#0067C0"/>
                                </Border>

                                <!-- Name + host + status -->
                                <StackPanel Grid.Column="1" Margin="10 0 0 0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Profile.Name}" FontWeight="SemiBold"
                                               FontSize="14" Foreground="#1A1A1A"/>
                                    <TextBlock FontSize="11" Foreground="#767676" Margin="0 1 0 0">
                                        <Run Text="{Binding Profile.Username, Mode=OneWay}"/>
                                        <Run Text="@"/>
                                        <Run Text="{Binding Profile.Host, Mode=OneWay}"/>
                                        <Run Text=":"/>
                                        <Run Text="{Binding Profile.Port, Mode=OneWay}"/>
                                        <Run Text="{Binding Profile.RemotePath, Mode=OneWay}" Foreground="#A0A0A0"/>
                                    </TextBlock>
                                    <StackPanel Orientation="Horizontal" Margin="0 3 0 0">
                                        <Ellipse Width="7" Height="7" VerticalAlignment="Center"
                                                 Fill="{Binding StatusColor}" Margin="0 0 5 0"/>
                                        <TextBlock Text="{Binding StatusText}" FontSize="11"
                                                   Foreground="{Binding StatusColor}"/>
                                        <TextBlock Text="{Binding ErrorText}" FontSize="11"
                                                   Foreground="#C42B1C" Margin="6 0 0 0"
                                                   Visibility="{Binding ErrorVisibility}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Action buttons -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal"
                                            VerticalAlignment="Center" Margin="8 0 0 0">
                                    <Button Content="{Binding MountButtonText}"
                                            Style="{StaticResource ActionButton}"
                                            IsEnabled="{Binding CanToggleMount}"
                                            Margin="0 0 6 0"
                                            Tag="{Binding}"
                                            Click="MountToggle_Click"/>
                                    <Button Content="{x:Static loc:Strings.BtnEdit}"
                                            Style="{StaticResource ActionButton}"
                                            Margin="0 0 6 0"
                                            Tag="{Binding}"
                                            Click="Edit_Click"/>
                                    <Button Content="{x:Static loc:Strings.BtnDelete}"
                                            Style="{StaticResource DangerButton}"
                                            Tag="{Binding}"
                                            Click="Delete_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty state -->
        <StackPanel x:Name="EmptyState" Grid.Row="1"
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="Collapsed">
            <TextBlock Text="{x:Static loc:Strings.NoConnections}" FontSize="16" Foreground="#767676"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="{x:Static loc:Strings.NoConnectionsHint}"
                       FontSize="12" Foreground="#A0A0A0" Margin="0 6 0 0"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0 1 0 0"
                Padding="16 8">
            <Grid>
                <TextBlock Text="{x:Static loc:Strings.FooterHint}"
                           FontSize="11" Foreground="#767676" VerticalAlignment="Center"/>
                <CheckBox x:Name="StartupCheckBox"
                          HorizontalAlignment="Right" VerticalAlignment="Center"
                          FontSize="11" Foreground="#404040"
                          Content="{x:Static loc:Strings.StartWithWindows}"
                          Checked="Startup_Changed" Unchecked="Startup_Changed"/>
            </Grid>
        </Border>
    </Grid>
</Window>
